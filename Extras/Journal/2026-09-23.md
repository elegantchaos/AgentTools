# 2026-09-23

- Moved the shared rules source to `runtimes/codex/rules`, matching the Agents repository's new `runtimes/` layout that makes room for Claude Code alongside Codex.
- Defined the shared rules path once in `RulesPublicTool.sharedRulesPath`.
- Changed the repository root marker from `codex/` to `runtimes/`.
- Claude Code does not read `~/.agents/skills`, so `agt skills link` now links into `~/.codex/skills` and `~/.claude/skills`, honouring `CODEX_HOME` and `CLAUDE_CONFIG_DIR`. `AGENTS_SKILLS_DIR` is gone.
- Linking now refuses to replace anything that is not a symlink; `~/.codex/skills` holds real Codex-installed skill directories that must not be deleted.
- Dropped the retired `refresh-public-skills` link cleanup, which only applied to `~/.agents/skills`.
- Open question: Codex's current docs list only `~/.agents/skills` for user skills; `~/.codex/skills` is assumed to still work.
