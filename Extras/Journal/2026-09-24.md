# 2026-09-24

- Moved `validate` from ReleaseTools (`rt`) into AgentTools as `agt validate`, released as 2.1.0. Validation serves agent-driven Swift work in general, so it belongs with the agent tooling, not the release tooling.
- Kept the command surface and behaviour identical so the move is verifiable against `rt validate`. Replaced the hand-rolled argument parser with ArgumentParser options (same flag names, `-c` kept for `--clean`); help and invalid-value errors now come from ArgumentParser.
- Split ReleaseTools' single 1250-line file into `ValidateCommand` (options), `ValidationTool` (flow), and small types for discovery, output shaping, Xcode destinations, and subprocesses.
- Renamed the private Xcode DerivedData directory to `.build/agt-validate/DerivedData`.
- Two quiet fixes during the port: captured subprocess output is read before waiting for exit (large `-showBuildSettings` JSON could otherwise fill the pipe and hang), and raw-mode output flushes buffered `print` output first so piped output stays in order.
- Removed the dead `skills/refresh-skill` repo-local skill source. Link tests now use a submodule fixture.
- Smoke-tested against JSONDump (workspace build and tests) and ClockSync (`--target` Xcode scheme fallback); output matched `rt validate` apart from xcodebuild's own line ordering.

## Plan for 2.2

- Validation must never share build directories with an IDE: concurrent builds stall on locks, and differing flags throw away each other's products. It either owns its build directory or hands the build to the IDE.
- SwiftPM: build into a private `--scratch-path` so validation stops fighting VS Code's Swift extension over `.build`.
- Xcode: when Xcode has the workspace open, build and test through Xcode's MCP bridge (`xcrun mcpbridge`) so validation reuses Xcode's own cache, falling back to `xcodebuild` with private DerivedData. Check the tool surface against Apple's documentation first; decide between an MCP client in `agt` (deterministic, preferred) and routing in the `swift:validation` skill.
- VS Code: no known external build/test API in the Swift extension; verify before ruling it out.
- Add fixtures in `Extras/`: a small Xcode project, a small SwiftPM library, possibly a VS Code workspace, for integration tests.
- Carry over the ReleaseTools roadmap item: run subprocesses through `Runner` and set `VALIDATING=1` for `swift` child processes so manifests can detect validation.
- Known limitation: warning detection matches `warning:` anywhere in a line, so test names or output containing that text flag a step as `[warnings]`.
- Known limitation: stdout is block-buffered when piped, so a thrown error's message can appear before earlier step output.

## Decision log

- Enabled a decision log in `Extras/Decisions/` and backfilled [0001](../Decisions/0001-standalone-agt-package.md)–[0004](../Decisions/0004-runtime-skill-links.md) and [0006](../Decisions/0006-toolchain-baseline.md) from earlier work. Today's move is [0005: `agt` owns Swift validation](../Decisions/0005-agt-owns-swift-validation.md).
- Deferred: "validation never shares a build directory with an IDE; it owns its own or hands the build to the IDE". It guides the 2.2 plan above but is not yet implemented; revisit when that work lands.

## Decision log follow-up

- Pruned the backfill to decisions that bind future `agt` work, rewritten to describe current policy: [0001: runtime skill links](../Decisions/0001-runtime-skill-links.md) and [0002: `agt` owns Swift validation](../Decisions/0002-agt-owns-swift-validation.md). The links in the section above point at the removed numbering.
- Removed as not decision-worthy for this repository: the standalone package (history, or evident from the repo), Agents root detection (implementation detail mirroring the Agents layout), and the toolchain baseline (stated in `Package.swift`).
- Removed as Agents repository policy, not `agt` policy: canonical shared rules in `runtimes/codex/rules` with a runtime-only `default.rules`. A candidate for the Agents repository if it starts a decision log.

## Source layout

- Grouped `Sources/AgentTools/` by responsibility, following ReleaseTools: `Commands/` (ArgumentParser subcommands), `Rules/`, `Skills/`, `Validation/`, and `Support/` (shared error, process-result, and root-locator types). The `@main` root command stays at the source root as application-level coordination, and each file still holds one type. Tests stay flat, as in ReleaseTools.
